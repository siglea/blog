---
layout: post
title:  "JVM 常用监控与诊断工具详解：从 jps 到 VisualVM"
date:   2020-03-27 23:25:00 +0900
comments: true
tags:
- Java
categories:
- 技术
---

在 Java 应用的开发和运维过程中，JVM 监控与诊断工具是排查性能问题、内存泄漏和线程死锁的必备利器。JDK 自带了一套功能强大的命令行工具，包括 jps、jstat、jinfo、jmap、jhat、jstack 等，配合 JConsole 和 VisualVM 等图形化工具，可以覆盖绝大多数 JVM 诊断场景。本文将逐一介绍这些工具的用法和实战示例。

---

### 一、jps：JVM 进程查看工具

`jps`（JVM Process Status Tool）用于列出当前系统中所有正在运行的 Java 进程，是其他所有 JVM 诊断工具的起点——你需要先知道目标进程的 PID。

```shell
jps
31856 xhs-push.jar
22640 Jps

jps -q
31856
22640

[root@VM_0_17_centos ~]# jps -m
31856 xhs-push.jar --push.queue=message_queue_low
22825 Jps -m

[root@VM_0_17_centos ~]# jps -l
31856 /home/web/xhs-push/xhs-push.jar
22886 sun.tools.jps.Jps

[root@VM_0_17_centos ~]# jps -v
31856 xhs-push.jar -Xms256m -Xmx512m -XX:+PrintGCDateStamps -XX:+PrintGC -Xloggc:...
22932 Jps -Dapplication.home=/usr/java/jdk1.8.0_171-amd64 -Xms8m

```

常用参数说明：
- `-q`：只输出 PID，省略主类名
- `-m`：输出传给 main 方法的参数
- `-l`：输出主类的完整包名或 JAR 路径
- `-v`：输出 JVM 启动参数

---

### 二、jstat：JVM 统计监控工具

`jstat`（JVM Statistics Monitoring Tool）可以实时监测系统资源占用与 JVM 运行情况，支持类加载、GC、编译等多维度的统计信息。

#### 2.1 命令语法

```
Usage: jstat -help|-options
       jstat -<option> [-t] [-h<lines>] <vmid> [<interval> [<count>]]
```

参数说明：
- `option`：统计选项，如 `-gcutil`、`-class` 等
- `-t`：输出时间戳
- `vmid`：目标 Java 进程号
- `interval`：采样间隔（秒或毫秒）
- `count`：打印次数，缺省则无限打印

#### 2.2 GC 输出字段含义

```
S0  — Heap上的 Survivor space 0 区已使用空间的百分比
S1  — Heap上的 Survivor space 1 区已使用空间的百分比
E   — Heap上的 Eden space 区已使用空间的百分比
O   — Heap上的 Old space 区已使用空间的百分比
P   — Perm space 区已使用空间的百分比
YGC — 从应用程序启动到采样时发生 Young GC 的次数
YGCT– 从应用程序启动到采样时 Young GC 所用的时间(单位秒)
FGC — 从应用程序启动到采样时发生 Full GC 的次数
FGCT– 从应用程序启动到采样时 Full GC 所用的时间(单位秒)
GCT — 从应用程序启动到采样时用于垃圾回收的总时间(单位秒)
```

#### 2.3 可用选项一览

```
## jstat -options
    -class 类加载统计
    -compiler 编译统计 
    -gc
    -gccapacity
    -gccause Allocation Failure
    -gcmetacapacity 元数据空间统计
    -gcnew
    -gcnewcapacity
    -gcold
    -gcoldcapacity
    -gcutil 总结垃圾回收统计
    -printcompilation JVM编译方法统计
```

#### 2.4 实战示例

**示例 1：垃圾回收统计（最常用）**

```
[root@VM_0_8_centos ~]# jstat -gcutil 27472 1000 5
  S0     S1     E      O      M     CCS    YGC     YGCT    FGC    FGCT     GCT
 78.04   0.00  14.10  61.31  96.88  94.27    288    5.612     4    3.335    8.947
 78.04   0.00  24.05  61.31  96.88  94.27    288    5.612     4    3.335    8.947
 78.04   0.00  34.14  61.31  96.88  94.27    288    5.612     4    3.335    8.947
 78.04   0.00  40.62  61.31  96.88  94.27    288    5.612     4    3.335    8.947
 78.04   0.00  47.20  61.31  96.88  94.27    288    5.612     4    3.335    8.947
```

从上面的输出可以看出：Eden 区（E）在每次采样间隔（1秒）内都在增长，说明有对象在持续分配；Old 区（O）占用 61.31% 保持稳定；共发生了 288 次 Young GC 和 4 次 Full GC。

**示例 2：类加载统计**

```
[root@VM_0_8_centos ~]# jstat -class -t 27472 1000 5
Timestamp       加载class的数量  所占用空间大小  未加载数量  未加载占用空间     Time(秒)
Timestamp       Loaded  Bytes  Unloaded  Bytes     Time
         5359.3  10847 20148.1        0     0.0      46.25
         5360.4  10847 20148.1        0     0.0      46.25
         5361.4  10847 20148.1        0     0.0      46.25
         5362.3  10847 20148.1        0     0.0      46.25
         5363.3  10847 20148.1        0     0.0      46.25
```

参考：jstat 命令使用详解 <https://www.cnblogs.com/lizhonghua34/p/7307139.html>

---

### 三、jinfo：Java 配置信息查看

`jinfo` 用于查看和动态修改 JVM 配置参数，在不重启应用的情况下调整某些 JVM 参数时非常有用。

```shell
 -flags               to print VM flags
 -sysprops            to print Java system properties
 <no option>          to print both of the above
```

---

### 四、jmap：内存映射工具

`jmap`（Memory Map For Java）是排查内存问题的核心工具，可以查看堆内存使用情况、生成堆转储文件、统计对象分布等。

#### 4.1 查看堆内存概况

```shell
## 查询某个pid进程对应的应用程序内存占用情况
jmap -heap pid
 
## 示例1
jmap -heap 5940

[root@VM_0_8_centos ~]# jmap -heap 27472
Attaching to process ID 27472, please wait...
Debugger attached successfully.
Server compiler detected.
JVM version is 25.171-b11

using thread-local object allocation.
Parallel GC with 2 thread(s)

Heap Configuration:
   MinHeapFreeRatio         = 0
   MaxHeapFreeRatio         = 100
   MaxHeapSize              = 2051014656 (1956.0MB)
   NewSize                  = 42991616 (41.0MB)
   MaxNewSize               = 683671552 (652.0MB)
   OldSize                  = 87031808 (83.0MB)
   NewRatio                 = 2
   SurvivorRatio            = 8
   MetaspaceSize            = 21807104 (20.796875MB)
   CompressedClassSpaceSize = 1073741824 (1024.0MB)
   MaxMetaspaceSize         = 17592186044415 MB
   G1HeapRegionSize         = 0 (0.0MB)

Heap Usage:
PS Young Generation
Eden Space:
   capacity = 37224448 (35.5MB)
   used     = 14139096 (13.484092712402344MB)
   free     = 23085352 (22.015907287597656MB)
   37.98335975324604% used
From Space:
   capacity = 3145728 (3.0MB)
   used     = 2929584 (2.7938690185546875MB)
   free     = 216144 (0.2061309814453125MB)
   93.12896728515625% used
To Space:
   capacity = 3670016 (3.5MB)
   used     = 0 (0.0MB)
   free     = 3670016 (3.5MB)
   0.0% used
PS Old Generation
   capacity = 165150720 (157.5MB)
   used     = 98856376 (94.27678680419922MB)
   free     = 66294344 (63.22321319580078MB)
   59.8582773359995% used

26452 interned Strings occupying 2659208 bytes.
```

从输出中可以清晰地看到各内存区域的容量、使用量和使用率，这对于判断是否需要调整 JVM 内存参数非常有帮助。

#### 4.2 对象统计与堆转储

```shell
## 示例2
jmap -F -histo 16464 > 16464.log
-histo[:live] 打印每个class的实例数目,内存占用,类全名信息. VM的内部类名字开头会加上前缀"*". 如果live子参数加上后,只统计活的对象数量. 

## 示例2
jmap -dump:format=b,file=outfile 3024可以将3024进程的内存heap输出出来到outfile文件里，再配合MAT（内存分析工具）。
```

`-histo` 可以快速定位哪些类的实例占用了最多内存；`-dump` 则生成完整的堆转储文件，用于离线分析。

---

### 五、jhat：堆转储分析工具

`jhat`（JVM Heap Analysis Tool）用于分析 jmap 生成的堆转储文件，并启动一个 HTTP 服务器供浏览器查看分析结果。

```shell
jhat outfile
http://localhost:7000
```

在实际工作中，更推荐使用 Eclipse MAT（Memory Analyzer Tool）来分析大型堆转储文件，因为 MAT 支持 OQL 查询、支配树分析等高级功能，比 jhat 更加强大。

---

### 六、jstack：线程堆栈分析工具

`jstack` 是排查线程问题（死锁、CPU 飙高、线程阻塞）的核心工具，它打印出指定 Java 进程中所有线程的堆栈信息。

```shell
# 示例
jstach 5940
Full thread dump Java HotSpot(TM) 64-Bit Server VM (25.92-b14 mixed mode):
 
"RMI TCP Connection(10)-10.2.13.162" #32 daemon prio=5 os_prio=0 tid=0x00000000179dc000 nid=0x1f60 in Object.wait() [0x000000001d7dd000]
   java.lang.Thread.State: TIMED_WAITING (on object monitor)
        at java.lang.Object.wait(Native Method)
        at com.sun.jmx.remote.internal.ArrayNotificationBuffer.fetchNotifications(ArrayNotificationBuffer.java:449)
        - locked <0x00000000d462ec18> (a com.sun.jmx.remote.internal.ArrayNotificationBuffer)
```

#### 排查 CPU 飙高的标准流程

这是线上环境中最常用的排查套路：

```shell
# 查看该进程中哪个线程占用大量 CPU，执行 top -H -p 11281 
# 找到执行时间最长的线程id：19279
# printf "%x\n" 19279 #输出十六进制 4b4f
# jstack 19268 |grep 4b4f
# 接下来我们将进程中的所有线程输出到一个文件中，执行：jstack  11281> jstack.txt
# 在进程中查找对应的线程 ID，执行：cat jstack.txt | grep -i 2c6d
# jstack -l 14716 |grep deadlock # 查看死锁
```

流程总结：`top -Hp` 找到高 CPU 线程 → 将线程 ID 转为十六进制 → 在 jstack 输出中定位该线程的堆栈 → 分析具体代码位置。

---

### 七、图形化工具

#### JConsole

JConsole 是 JDK 自带的 JMX 监控工具，提供了内存、线程、类加载、MBean 等多维度的实时监控。适合开发环境使用。

<img src="{{ site.baseurl }}/img/jvm-util/jconsole.jpg" width="600px" />

#### VisualVM

VisualVM 是功能更全面的 JVM 监控工具，集成了 jstat、jmap、jstack 等多个命令行工具的功能，还支持插件扩展（如 Visual GC 插件），是本地开发和测试环境的首选工具。

---

### 八、其他工具

#### javap：字节码查看工具

`javap` 可以反编译 `.class` 文件，查看 Java 字节码指令。这对于理解 JVM 执行机制、分析编译器优化非常有帮助。

<img src="{{ site.baseurl }}/img/jvm-util/javap.jpg" width="600px" />

#### HSDIS：JIT 反汇编插件

HSDIS 是 HotSpot VM 的反汇编插件，可以将 JIT 编译器生成的本地机器码反汇编为可读的汇编指令，用于深度性能分析。

<img src="{{ site.baseurl }}/img/jvm-util/hsdis.jpg" width="600px" />

---

### 九、总结

JVM 的监控和诊断工具链非常完善，从进程查看（jps）、统计监控（jstat）、配置查看（jinfo）、内存分析（jmap + jhat）、线程分析（jstack），到图形化的 JConsole 和 VisualVM，形成了一个完整的诊断闭环。掌握这些工具的核心用法，能够在生产环境中快速定位和解决大部分 JVM 相关的性能问题。

建议在日常开发中养成使用这些工具的习惯，而不是等到线上出现故障时才临时查阅文档。

#### 参考

- JVM 运行时内存使用情况监控：<https://blog.csdn.net/zmx729618/article/details/78392715>
